# Instrucciones de uso

Ejecutar el generador "generar-compose.sh" con los clientes deseados
Ejecutar "make docker-compose-up"

# Solución al ejercicio 2
En helper-scripts/generator.py se agregan volúmenes tipo bind mount (usando la sintaxis de dos puntos ':') para cada
script de configuración.